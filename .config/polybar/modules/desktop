[module/barrier]
type = custom/ipc

hook-0 = $POLYBAR_SCRIPTS/barrier
hook-1 = echo "   $DEVICE   "
initial = 1

click-left = remote zeus

format-padding = 0
format-background = ${colours.blue}
format-foreground = ${colours.background}

; vim:ft=dosini
